---
- name: mail test
  gather_facts: no
  hosts: all
  connection: localhost
  
  tasks:
    - name: Make Result Directory
      become: yes
      archive:
        path:
        - /var/lib/awx/projects/_11__ljw90/backup/$(date +%Y%m%d)/
        dest:
        - /var/lib/awx/projects/_11__ljw90/backup/$(date +%Y%m%d)/config.zip
        format: zip
        ignore_errors: true
